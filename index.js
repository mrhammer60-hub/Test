import React from 'react';
import ReactDOM from 'react-dom/client';
import IronCoach from './IronCoach';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <IronCoach />
  </React.StrictMode>
);
